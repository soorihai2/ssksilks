# Server Configuration
# In production, this should match your Plesk/hosting configuration
PORT=3001
# Use 'production' in live environment
NODE_ENV=development

# Frontend URL
# In production, this should be your domain
# Example: FRONTEND_URL=http://ssksilks.in
FRONTEND_URL=http://localhost:4173


# Production Notes:
# 1. Ensure PORT matches your hosting provider's configuration
# 2. Set NODE_ENV=production
# 3. Use HTTPS URLs in production
# 4. Use real domain name instead of localhost
# 5. Use live API keys instead of test keys 